# Unabhängiger Vergleich der zwei neuen B28-Schlusslisten

Beide Änderungen sind reine Anzeigeänderungen und bestehen den unabhängigen Parser-/Quellvergleich gegen `B28-isomorphism-examples.before.tex`.

- `SemigroupIsoIdentityZeroConditions`: Die Neutralitätsäquivalenz und die Nulläquivalenz erhalten je einen eigenen Punkt (i)/(ii). Ihre vollständigen Äquivalenzen einschließlich aller Konjunktionen bleiben unverändert. Die gemeinsamen Voraussetzungen `f:A≅B` und `e,z∈A` stehen einmal davor. Die Fortsetzungszeilen gehören jeweils eindeutig zur davor begonnenen Äquivalenz.
- `SemigroupTransportedRelationCriterion`: Die beiden universalen Relationskriterien erhalten je eine eigene Zeile (i)/(ii). Die gemeinsamen Voraussetzungen `f:A≅B` und `ρ⊆A×A` stehen einmal davor. Die Variablenkommas in `∀u,v` beziehungsweise `∀a,b` bleiben unangetastet.

Der Prüfer entfernt für den Inhaltsvergleich ausschließlich die neu eingeführten Teilnummern, Ausrichtungs-/Abstandsbefehle und die früheren Listentrennkommas vor dem Zeilenende. Danach sind die mathematischen Anzeigetokens identisch. Beide Anzeigen haben genau die Nummernfolge (i), (ii).

Nach Maskierung ausschließlich dieser beiden ersten Anzeige-Argumente ist die gesamte vorherige und aktuelle Datei exakt identisch. Dies umfasst alle separaten Strukturargumente, IDs, Titel, Kontexte und Beweise. Zusätzlich wurden alle 2.327 Beweistabellenzeilen als vollständige Quellfragmente verglichen; sie sind exakt unverändert. Keine Quelle geändert, kein Build ausgeführt.

Beleg mit vollständigen Vorher-/Nachher-Anzeigen und Dateihashes: `b28-two-displays-independent-review.json`. Reproduzierbarer Prüfer: `review-b28-two-displays.py`. Ergebnis: keine Befunde.
